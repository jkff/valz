#!/usr/bin/env jython
# vim:set ft=python:

import sys
import doctest

if len(sys.argv) == 0:
    print >> sys.stderr, 'no files specified'
    sys.exit(1)

for filename in sys.argv[1:]:
    doctest.testfile(filename)

